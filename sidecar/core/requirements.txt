pydantic>=2.0
anthropic>=0.30
httpx>=0.27
# Verifies TLS against the OS trust store. Required for SAP systems behind
# corporate PKI, whose chains Windows trusts but certifi does not ship.
truststore>=0.9
pytest>=8.0
